# Server
server.port=8081

# DataSource
spring.datasource.url=jdbc:mysql://localhost/demo_oauth2_server?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=dev*I(O)P

# JPA
spring.jpa.hibernate.ddl-auto=create-drop

# Logging
logging.level.org.hibernate=warn
logging.level.org.hibernate.SQL=debug
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=trace
logging.level.com.example=debug

# Security
security.user.name=user
security.user.password=password

# OAuth2Server
security.oauth2.client.client-id=foo
security.oauth2.client.client-secret=bar
security.oauth2.authorization.check-token-access=isAuthenticated()


